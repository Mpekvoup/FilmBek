# FilmBek - Кинематографический портал

FilmBek — это веб-приложение на Django для просмотра информации о фильмах, сериалах и новостях кинематографии. Приложение предоставляет пользователям возможность поиска, фильтрации контента по различным критериям, управления списками просмотра и оценки фильмов.

## 🎬 Основные функции

- **Каталог фильмов и сериалов** - Полная база данных фильмов и сериалов с детальной информацией
- **Поиск и фильтрация** - Быстрый поиск по названиям с подсказками, фильтрация по жанрам, странам, рейтингам, годам
- **Система оценок** - Возможность выставлять оценки фильмам и сериалам (0-10)
- **Новости кино** - Ленты новостей о кинематографии
- **Профиль пользователя** - Регистрация, авторизация, управление профилем
- **Список просмотра** - Добавление фильмов в список для просмотра
- **Адаптивный дизайн** - Полная поддержка мобильных устройств

## 📋 Технологический стек

- **Backend**: Django 5.1.5
- **Database**: SQLite3
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla JS)
- **Python**: 3.12
- **ОС**: Поддержка Windows, Linux, macOS

## 🚀 Установка и запуск

### Предварительные требования

- Python 3.12+
- pip (менеджер пакетов Python)
- Git

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd FilmBek
```

### Шаг 2: Создание виртуального окружения

```bash
# Для Windows
python -m venv venv_new
venv_new\Scripts\activate

# Для Linux/macOS
python3 -m venv venv_new
source venv_new/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

Если файл `requirements.txt` отсутствует, установите необходимые пакеты вручную:

```bash
pip install Django==5.1.5
pip install pytils
```

### Шаг 4: Переход в директорию проекта

```bash
cd FilmbekProject
```

### Шаг 5: Применение миграций базы данных

```bash
python manage.py migrate
```

### Шаг 6: Создание суперпользователя (администратор)

```bash
python manage.py createsuperuser
```

Следуйте инструкциям в консоли для создания учетной записи администратора.

### Шаг 7: Сбор статических файлов (опционально)

```bash
python manage.py collectstatic
```

### Шаг 8: Запуск разработчика сервера

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000`

## 📁 Структура проекта

```
FilmBek/
├── .gitignore                          # Git исключения (venv, __pycache__, .env)
├── FilmbekProject/                    # Основная директория Django проекта
│   ├── FilmApp/                       # Основное приложение Django
│   │   ├── models.py                  # Модели БД (Film, Serial, News, etc.)
│   │   ├── views.py                   # Представления (views)
│   │   ├── urls.py                    # URL маршруты
│   │   ├── forms.py                   # Django формы
│   │   ├── admin.py                   # Конфигурация Django админ-панели
│   │   ├── static/                    # Статические файлы (CSS, JS)
│   │   │   ├── css/                   # CSS стили
│   │   │   ├── js/                    # JavaScript файлы
│   │   │   ├── img/                   # Изображения
│   │   │   └── videos/                # Видео файлы
│   │   ├── templates/                 # HTML шаблоны
│   │   │   ├── MainPage.html          # Главная страница
│   │   │   ├── AllFilms.html          # Каталог фильмов
│   │   │   ├── AllSerial.html         # Каталог сериалов
│   │   │   ├── MovieDetail.html       # Страница фильма
│   │   │   ├── SerialDetail.html      # Страница сериала
│   │   │   ├── search_results.html    # Результаты поиска
│   │   │   ├── profile.html           # Профиль пользователя
│   │   │   ├── auth.html              # Страница входа/регистрации
│   │   │   └── ...                    # Другие шаблоны
│   │   ├── migrations/                # Миграции БД
│   │   └── __pycache__/               # Python кэш (в .gitignore)
│   ├── FilmbekProject/                # Конфиг Django проекта
│   │   ├── settings.py                # Основные настройки
│   │   ├── urls.py                    # Главные URL маршруты
│   │   ├── asgi.py                    # ASGI конфигурация
│   │   └── wsgi.py                    # WSGI конфигурация
│   ├── static/                        # Глобальные статические файлы
│   ├── staticfiles/                   # Собранные статические файлы (collectstatic)
│   ├── media/                         # Пользовательские медиа файлы
│   ├── manage.py                      # Django утилита управления
│   ├── db.sqlite3                     # База данных SQLite
│   └── filmapp_data.json              # Данные приложения (JSON)
├── venv_new/                          # Виртуальное окружение (в .gitignore)
└── check/                             # Тестовые/проверочные файлы
```

## 🗄️ Модели данных

### Film (Фильм)
- **name** - Название фильма
- **description** - Описание
- **year** - Год выхода
- **image_url** - URL постера
- **rating** - Оценка (ForeignKey на Rating)
- **genres** - Жанры (ManyToMany)
- **countries** - Страны (ManyToMany)
- **age_rating** - Возрастной рейтинг
- **status** - Статус
- **slug** - URL-friendly идентификатор

### Serial (Сериал)
Аналогичен Film с дополнительными полями для сериальной информации

### News (Новость)
- **title** - Заголовок
- **content** - Содержание новости
- **image_url** - Изображение новости
- **published_date** - Дата публикации
- **author** - Автор (ForeignKey на User)

### UserRating (Оценка пользователя)
- **user** - Пользователь (ForeignKey)
- **film/serial** - Фильм или сериал
- **value** - Значение оценки (0-10)
- **created_at** - Дата создания

### Genre, Country, AgeRating
Справочники для категоризации контента

## 🔑 Основные endpoints (URL маршруты)

| URL | Метод | Описание |
|-----|-------|---------|
| `/` | GET | Главная страница |
| `/films/` | GET | Все фильмы |
| `/serials/` | GET | Все сериалы |
| `/movie/<id>/` | GET | Детали фильма |
| `/serial/<id>/` | GET | Детали сериала |
| `/search/` | GET | Поиск контента |
| `/search-suggestions/` | GET | AJAX подсказки поиска |
| `/films/filter/<type>/<value>/` | GET | Фильтрация фильмов |
| `/serials/filter/<type>/<value>/` | GET | Фильтрация сериалов |
| `/news/` | GET | Все новости |
| `/news/<id>/` | GET | Детали новости |
| `/profile/` | GET | Профиль пользователя (требует auth) |
| `/watchlist/` | GET | Список просмотра |
| `/login/` | GET, POST | Вход/регистрация |
| `/logout/` | GET | Выход |
| `/admin/` | GET, POST | Django админ-панель |

## ⚙️ Конфигурация

### settings.py основные настройки:

```python
# Язык
LANGUAGE_CODE = 'ru'
TIME_ZONE = "Asia/Almaty"

# Debug режим (отключить в production)
DEBUG = True

# Database
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "db.sqlite3",
    }
}
```

### Переменные окружения (.env)

Создайте файл `.env` в корне проекта (см. `.gitignore`):

```
DEBUG=False
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
```

## 🔐 Безопасность

> ⚠️ **Важно**: Перед развертыванием на production:

1. Изменить `SECRET_KEY` в settings.py
2. Установить `DEBUG = False`
3. Добавить ваши домены в `ALLOWED_HOSTS`
4. Использовать переменные окружения для чувствительных данных
5. Использовать HTTPS
6. Установить `SECURE_SSL_REDIRECT = True`

## 📦 Зависимости

```
Django==5.1.5
pytils  # Для кириллицы в slugify
```

Для установки всех зависимостей:

```bash
pip install -r requirements.txt
```

## 🧪 Тестирование

```bash
cd FilmbekProject
python manage.py test
```

## 🐛 Известные проблемы и решения

### Ошибка при поиске: "SyntaxError: Unexpected token '<'"
**Решение**: Убедитесь, что эндпоинт `/search-suggestions/` возвращает валидный JSON.

### Виртуальное окружение попадает в git
**Решение**: Проверьте `.gitignore` в корне репозитория. Оно должно содержать:
```
venv/
venv_new/
```

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 👥 Контрибьютинг

Для внесения изменений:

1. Создайте новую ветку (`git checkout -b feature/AmazingFeature`)
2. Совершите изменения (`git commit -m 'Add some AmazingFeature'`)
3. Отправьте в репозиторий (`git push origin feature/AmazingFeature`)
4. Откройте Pull Request

## 📧 Контакты

- Email: [ваш email]
- GitHub: [ваш профиль]

## 📚 Полезные ресурсы

- [Django документация](https://docs.djangoproject.com/)
- [Django REST framework](https://www.django-rest-framework.org/)
- [pytils документация](https://pytils.readthedocs.io/)

---

**Статус проекта**: 🚧 В разработке

**Последнее обновление**: 4 декабря 2025 г.
